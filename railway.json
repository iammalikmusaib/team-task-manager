{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run install:all && npm run build && npm start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}
