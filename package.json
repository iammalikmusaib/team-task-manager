{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready Team Task Manager with React, Express, MongoDB, JWT auth, and RBAC.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "playwright": "^1.59.1"
  }
}
